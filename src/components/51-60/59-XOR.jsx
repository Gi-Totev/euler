import React from "react";
import InputBox from "../InputBox/InputBox";

const XOR = ({ classes }) => {
  return (
    <InputBox
      classes={classes}
      onClick={null}
      result={null}
      resultText={"Unfinished"}
    />
  );
};

export default XOR;
